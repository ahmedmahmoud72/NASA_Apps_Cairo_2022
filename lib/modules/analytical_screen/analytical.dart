import 'package:flutter/material.dart';

class Analytical extends StatelessWidget {
  const Analytical({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child:  Text(
        'Analytical'
      ),
    );
  }
}
